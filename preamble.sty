\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{preamble}[2025/07/29 Luca Paganin awesome-cv custom preamble]

%------------------------------------------------------------
% packages and geometry
%------------------------------------------------------------
% Your package content here
\usepackage{ifthen}
\usepackage[english,italian]{babel}
% \usepackage{lmodern}

% Configure page margins with geometry
\geometry{left=2cm, top=1.5cm, right=2cm, bottom=2cm, footskip=.5cm} 
%------------------------------------------------------------


%------------------------------------------------------------
% FONTS
%------------------------------------------------------------
\fontdir[fonts/] % Specify the location of the included fonts
% Include our custom font configuration
%------------------------------------------------------------


%------------------------------------------------------------
% COLORS 
%------------------------------------------------------------
% Color for highlights
\colorlet{awesome}{awesome-red} % Default colors include: awesome-emerald, awesome-skyblue, awesome-red, awesome-pink, awesome-orange, awesome-nephritis, awesome-concrete, awesome-darknight
%\definecolor{awesome}{HTML}{CA63A8} % Uncomment if you would like to specify your own color

% Colors for text - uncomment and modify
%\definecolor{darktext}{HTML}{414141}
%\definecolor{text}{HTML}{414141}
%\definecolor{graytext}{HTML}{414141}
%\definecolor{lighttext}{HTML}{414141}
%------------------------------------------------------------

%------------------------------------------------------------
% Global variables for bilingual content
%------------------------------------------------------------
% Locations
\def\genoaLoc{\ifenglish{Genoa, Italy}\else{Genova, Italia}\fi}
\def\savonaLoc{\ifenglish{Savona, Italy}\else{Savona, Italia}\fi}
% Organizations
\def\unigeOrg{\ifenglish{University of Genoa, Italy}\else{Università degli Studi di Genova, Italia}\fi}
\def\msAuthority{Microsoft}
\def\msCertified{Microsoft Certified}
%------------------------------------------------------------


%------------------------------------------------------------
% new and renew commands
%------------------------------------------------------------
\renewcommand{\acvHeaderSocialSep}{\quad\textbar\quad} % If you would like to change the social information separator from a pipe (|) to something else

%------------------------------------------------------------
\endinput